import React from "react";

const Header = () => (
  <header className="w-full bg-white/80 backdrop-blur shadow-sm py-6 mb-0">
    <div className="max-w-3xl mx-auto px-4 flex items-center justify-center">
      <span className="text-3xl font-extrabold tracking-tight select-none">
        <span className="bg-gradient-to-r from-blue-800 to-blue-600 bg-clip-text text-transparent">Maritime Resume Checker</span>
      </span>
    </div>
  </header>
);

export default Header;